{
  "name": "aone-shop-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LAN shop server — shared offline bills for biller + cashier (multi-PC same branch)",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
